<%= render partial: 'forms/error', locals: {errors: @file.errors} %>

<%= form_for @file do |f|%>
		<%= f.collection_select :event_ids,
			Event.all,
			:id,
			:body,
			{:selected=>@file.event_ids},
			{:multiple=>true}
		%>
		<%= f.submit %>
<% end %>