<%= render partial: 'forms/error', locals: {errors: @case_file.errors} %>

<%= form_for @case_file do |f|%>
	<%= f.collection_select	:event_ids,
	Event.all,
	:id,
	:tagline,
	{:selected=>@case_file.event_ids},
	{:multiple=>true}
	%>
	<%= f.submit %>
<% end %>
